# testing out github stacked prs

## a

created branch `a` from `main`.

add some features

## b

created branch `b` from `a`

this is on top `a` and so has the changes of `a` as usual.

## c

another branch that builds on top of the work of branch `b` which also builds on top of branch `a`